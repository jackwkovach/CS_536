int x; // varDecl: type id SEMICOLON
int y;

struct a b; // varDecl: STRUCT id id SEMICOLON

int f(){}  //fnDecl: type id formals fnBody
int f(int b, void c){}
int k(int a, int b, int c){ int d; int f; struct foo bar;}


 struct a{  // structDecl: STRUCT id LCURLY structBody RCURLY SEMICOLON
       int a; int c;

       	          struct foo bar;
		  };

void foo(int b){ // test all the stmt
     a = b;
     a++;
     b--; 
     cin >> a;
     cout << d;

     if(g > f){}

     if( a < b){
     	 int a;
	 g = f;
      }

      if( a == b){
      	  int a;
	  g = f;
      }     
else{}

while(a ==b ){}

a = c-d;
b = f +p + 3; // int lit

// test all kinds of strings

// test dot access
c = d.g;
//return a;

// return ;
//f();
//f(b,c);

}