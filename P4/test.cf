int global;
bool b;

struct aa{
       int aai;
};
		
struct bb{
       struct aa aas;
       int aa;
       int akk;
};

struct cc{
       struct bb bbs;
       int l;
};

struct sss{
       int d;
       int e;
       int k;
       bool f;	   
};


void main(int a, bool b){
     int k;
     int cc;   # this won't corrupt with struct cc declaration!!! 
     struct aa his;
     struct sss abc;
     struct aa lv1;
     struct bb lv2;
     struct cc lv3;
     int aa;

    # struct declaration
#     struct sss oo;
#     struct ss ooo;
#     	         struct sks oo;   # wrong! invalid struct type
     k = -3;
     k = lv1.aai;
     k = lv3.bbs.aas;

     #exciting one
 
     # test if statement
     # test global variable
     if(global > 0){
     	int bb;
	struct bb lvlv;
     # test variable use inside if
     # test variable use from mixed scopes
	bb = global - 3 + k;
	k = lv2.aas;
     }

     # test if statement
     # test global variable
     if(k > 0){
     	int k; # it's ok, it's in new scope now
	# try some serious embedded statement ! ! !	
	if(k > 0){
	     int k;
	     if(k > 0){
	     	  int k;
	     }
	}
     }else{
     	    int elseelse;
	    while(elseelse > 0){
	       int kk;
	       while(kk > 0){
		   int kk;
	 	 }
	    }
     }

}


# shouldn't dead
#int main(){


#}

