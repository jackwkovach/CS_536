int gi;
struct point{
       int x;
       int y;
};

struct space{
       struct point p;
       int z;
};

struct wtf{
     struct space s;
     struct point p;  

     int z;

};


void main(int af, int bf){
     int k;
     struct point p;
     struct space s;
     struct wtf w;
     # p.x = k;
     # p.y = k;
     # cout<<"x: ";
     # cout<<p.x;
     # cout<<"\ny: ";
     # cout<<p.y;

    return;
}

